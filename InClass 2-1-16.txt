branches - 
git branch name
git checkout name

pull request merges branches back into master
while in the branch your editing
git merge nameOfBranchToMergeWith
git push origin branchName

git add -a (adds all files in directory)

.gitignore  (file containing names of files you want github to ignore, accepts wildcards)

.gitignore_global

git revert changenumber

git reset (resets to a certain commit - anything after the commit is removed, no way of undoing this process)